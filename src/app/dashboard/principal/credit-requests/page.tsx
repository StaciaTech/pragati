'use client';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function PrincipalCreditRequestsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Credit Requests</CardTitle>
        <CardDescription>Approve or reject credit requests from your TTCs.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>This page is under construction.</p>
      </CardContent>
    </Card>
  );
}
